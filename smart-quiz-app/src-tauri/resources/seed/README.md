# seed/ —— 题库数据包（不入库）

此目录存放 `.smartbank` 题库包，应用启动时自动导入内置种子。

**版权原因，数据文件不随本仓库分发**。两种方式获得题库：

1. **应用内导入**（推荐）：题库管理 → 导入题库包（`.smartbank`），或用 Excel 模板自建
2. **放回本目录再构建**：拿到数据包后放入此目录，`npm run tauri build` 会随安装包分发

数据包生成流水线见仓库根 `scripts/`（需自备源资料）。
