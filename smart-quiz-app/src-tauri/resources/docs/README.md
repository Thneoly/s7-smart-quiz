# docs/ —— 检索语料包（不入库）

此目录存放 `docs.docpack`（官方文档提取语料的打包），用于资料速查的全文检索索引构建。

**版权原因，语料文件不随本仓库分发**。两种方式获得：

1. **应用内导入**：资料速查 → 导入语料包（`.docpack`）→ 重建索引
2. **放回本目录再构建**：拿到数据包后放入此目录，安装包将内置分发

数据包生成流水线见仓库根 `scripts/`（需自备源资料）。
