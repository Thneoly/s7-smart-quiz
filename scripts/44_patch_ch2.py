# -*- coding: utf-8 -*-
"""用重生成版替换 lectures.json 第2章，并检查 V区范围错误的蔓延情况"""
import json, sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

CH2 = {
  "no": 2,
  "title": "编程基础",
  "intro": "本章是后续指令学习的地基：定时器、传送、比较与通信指令都建立在存储区、数据类型与寻址之上。\"存储区与寻址\"专题几乎全部考这些内容，学完应能说出任意地址属于哪个区、占几字节、指向哪一位。",
  "sections": [
    {"h": "三种编程语言：LAD、FBD 与 STL",
     "paras": [
       "Micro/WIN SMART 提供三种编程语言。梯形图 LAD 由继电器控制电路演变而来，触点表示输入条件、线圈表示输出结果、方框表示定时器计数器等指令，程序从左到右、自上而下执行，直观易懂，使用最普遍。",
       "功能块图 FBD 源自数字电路的逻辑门图，没有触点线圈，指令全以方框呈现、以\"逻辑流\"传递控制。语句表 STL 是类似汇编的文本语言，靠助记符和逻辑栈编程，使用较少，但能实现 LAD 和 FBD 无法编写的功能；LAD/FBD 程序总能用 STL 查看，反之不一定。"],
     "ref": "手册章节\\06_编程概念.txt·6.3 创建用户程序（LAD/FBD/STL 编辑器特点）"},
    {"h": "存储区地图：映像区与内部存储区",
     "paras": [
       "存储区分外部映像区与内部存储区。数字量输入 I 在扫描周期开始被采样到过程映像，输出 Q 在周期末集中写出；模拟量 AI、AQ 不随周期刷新，访问时立即读写，必须用偶数地址（AIW0、AQW4），AI 只读、AQ 只写。",
       "内部区各有分工：V 存全局中间结果，M 作内部控制继电器，T、C 兼具状态位和 16 位当前值，HC 是只读 32 位计数值，AC0~AC3 为四个 32 位累加器，SM 提供特殊功能位，L 是每 POU 独享的 64 字节局部区，S 供顺控用。必背：I、Q、M 均 0.0~31.7，T、C 各 256 个，SM 到 SM2047.7，S 到 S31.7；V 区随机型递增，V2.8 中 SR/ST20 为 VB12287、SR/ST60 达 VB24575，CR 系列到 VB8191。"],
     "ref": "系统手册v28.txt·附录 E.5 存储器范围和特性"},
    {"h": "数制与数据类型：从位到双字",
     "paras": [
       "CPU 内一切数据以二进制存放，常数可写成二进制、十六进制（仅用 0~9 和 A~F）、ASCII 等格式，如 2#1010_0101、16#4E4F；三制互化要会，如 2#1100 1000 = 200 = 16#C8。",
       "按长度分：BOOL/BYTE/WORD/DWORD 分别占 1 位、1 字节、2 字节、4 字节；无符号字节、字、双字为 0~255、0~65535、0~4294967295，有符号为 -128~+127、-32768~+32767。实数按 32 位 IEEE 754 存放、只能按双字访问，精确到小数点后第 6 位，浮点常数最多 6 位小数。"],
     "ref": "手册章节\\05_PLC概念.txt·5.2.2 实数格式、5.2.4 常数值与表格 5-3 数据长度范围"},
    {"h": "地址的构成：位、字节、字与双字",
     "paras": [
       "位地址由存储区标识符、字节地址和位号组成：M3.6 即 M 区 3 号字节 6 号位，V1.3 即 V 区 1 号字节 3 号位；字节、字、双字再加长度代号与起始字节号，如 VB20、VW30、VD50。长度决定覆盖范围：VW2=VB2+VB3，VD1=VB1~VB4，MW0、MD0 同理。",
       "多字节数据的最高有效位 MSB 排在最左。T、C 地址不带长度代号，用于触点访问状态位、用于字指令访问当前值；HC 只能按双字只读访问，累加器按字节或字访问时只取低 8 位或低 16 位。"],
     "ref": "手册章节\\05_PLC概念.txt·5.2 访问数据（位寻址与各存储区的绝对寻址）"},
    {"h": "三种寻址方式与符号命名规则",
     "paras": [
       "直接寻址用标识符加长度与地址指明操作数，如 I0.0；符号寻址给地址或常量定义符号名，如用\"启动按钮\"代替 I0.0 访问，常量同样可定义符号。符号名可用字母、数字、下划线和扩展字符，首字符不能是数字，不得使用关键字，长度不超过 23 个字符。",
       "间接寻址用指针访问数据：指针是存放另一单元地址的双字，只能用 V、L 单元或 AC1~AC3 充当；建指针 MOVD &VB200, AC1，取数据 MOVW *AC1, AC0。指针可访问 I、Q、V、M、S、AI、AQ、SM 及 T、C 的当前值，但不能访问单个位及 HC、L、AC 区；改指针时字节加 1、字加 2、双字加 4。"],
     "ref": "手册章节\\05_PLC概念.txt·5.2.6 使用指针进行间接寻址"}
  ],
  "exam_tips": [
    "地址换算是高频送分题：M3.6 是 3 号字节第 6 位，VW2=VB2+VB3，VD1=VB1~VB4；16#12345678 存双字时高字 16#1234、低字 16#5678，MSB 在最左；AIW/AQW 只能使用偶数地址。",
    "数据类型范围成对背：无符号字节 0~255、字 0~65535，有符号字 -32768~+32767；浮点数只精确到小数点后第 6 位、浮点常数最多指定 6 位小数，\"100000000+1 结果不变\"是经典判断题。",
    "存储区范围题注意型号与版本：I/Q/M 都是 0.0~31.7，T、C 各 256 个，AIW/AQW 到 110，SM 到 SM2047.7，S 到 S31.7；V 区大小随机型递增，作答时以题目指定的 CPU 型号为准。",
    "语言与指针对比必考：LAD 由继电器电路演变、最普遍，FBD 由数字电路演变，STL 类似汇编且能实现另两种语言无法实现的功能；作指针只能用 V、L 和 AC1~AC3，且不能间接访问单个位。"
  ]
}

P = '../smart-quiz-app/src/study/lectures.json'
L = json.load(open(P, encoding='utf-8'))['lectures']
L = [CH2 if l['no'] == 2 else l for l in L]
assert sum(1 for l in L if l['no'] == 2) == 1
json.dump({'lectures': sorted(L, key=lambda x: x['no'])}, open(P, 'w', encoding='utf-8', newline='\n'), ensure_ascii=False, indent=1)
print('第2章已替换')

# V区范围错误蔓延检查：guide.json 与 lectures 各章里的 8191
import re
guide = open('../smart-quiz-app/src/study/guide.json', encoding='utf-8').read()
print('guide.json 含 8191 的行数:', len([l for l in guide.splitlines() if '8191' in l]))
for line in guide.splitlines():
    if '8191' in line: print('  guide:', line.strip()[:100])
alltext = ' '.join(p for l in L for s in l['sections'] for p in s['paras'])
print('lectures 含 8191:', '8191' in alltext)
