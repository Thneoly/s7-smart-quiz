# S7-200 SMART 初级认证学习指南

> 依据官方培训课程（基础篇 7 章 + 中级篇 6 章 + 高级篇 9 章，共 22 章）整理
> 每章含学习目标、核心要点、配套题库练习（在应用「学习模式」中可点击直达）、手册出处

## 基础篇

> S7-200 SMART 编程入门第一阶：认识硬件组成与选型方法、建立存储区/数据类型/寻址概念、掌握 Micro/WIN SMART 软件操作与程序下载、熟练运用位逻辑/定时器/计数器/传送/数学/比较/转换七类基础指令，并通过实战项目与程序调试形成独立编程能力；覆盖初级认证全部必考内容（第1/2/4/5章必考，第3/6章结合学习箱实操），课程总时长约29.9小时。

### 第1章 硬件介绍（251 分钟 · 核心）

**学习目标**：能听懂所有内容；熟知硬件的功能；会查询相关资料；根据要求选择合适的硬件；课后题正确率在80%以上

**核心要点**
- 产品特点：单体CPU本体I/O点数最高60点；分标准型（SR/ST）与经济型供选型；基本指令执行时间0.15μs；本体标配以太网口用于程序下载和与CPU/触摸屏/计算机组网
- CPU型号命名：SR=继电器输出、ST=晶体管输出，数字为I/O点数；SR20为12入/8出，ST60为36入/24出；电源85~264V AC或20.4~28.8V DC；数字量输入支持漏型/源型接线，接地理想时建议将传感器M端与PE连接
- 输出类型对比：继电器输出可驱动大功率设备但不能频繁动作；晶体管输出可高频率输出（本体最多3路高速脉冲输出，频率高达100kHz，支持PWM/PTO及运动包络）但不能驱动大功率设备
- 本体继承Micro SD卡插槽：用通用Micro SD卡即可实现程序传输、固件升级、恢复出厂设置；标准型CPU断电后时钟可保持7天
- 数字量扩展模块：EM DR16/DR32继电器输出触点额定2A、公共端8A，输出回路电压5~30V DC或5~250V AC；EM DT16/DT32晶体管(MOSFET源型)输出0.75A，20.4~28.8V DC；电缆最长500m（屏蔽）/150m（非屏蔽）
- 数字量输入接线规则：同一组输入点接线类型（漏型/源型）必须相同，不同组可以不同；PNN/NPN光电开关对应不同接法（PNP接源型、NPN接漏型）
- 模拟量输入EM AE04/AE08：电压模式12位+符号位、电流模式12位分辨率；信号类型±10V/±5V/±2.5V/0~20mA；满量程-27648~27648（0~20mA对应0~27648，4~20mA对应5530~27648）；精度电压模式±0.1%/±0.2%；未使用的输入通道应短路
- 分辨率决定最小可分辨变化量（12位、0~20mA、量程0~100℃时温度最小变化100/4096=0.02℃）、精度决定与实际值的偏差；高精度必为高分辨率，高分辨率不代表高精度
- 热电阻EM AR02/AR04（可测PT100/PT1000/Ni100/Cu100等，四线制测温最准）与热电偶EM AT04（J/K/T/E/R&S/N型）：温度采集值为实际值的10倍（采集521→52.1℃），电阻采集值最大27648
- 信号板系列：SB DT04（2入2出，MOSFET源型0.5A）、SB AE01/AQ01（11位+符号位）、SB BA01电池板（电池CR1025需自购，保持1年，临界电压2.5V）、SB CM01串口板（RS485带隔离中继1000m/无中继50m、最大187.5k，RS232为10m）；注意西门子Tx/B=485信号+、Rx/A=485信号-，与多数第三方A/B定义相反

**配套练习**：硬件与选型、数字量与模拟量IO

**手册出处**：系统手册v2.8 第1章 产品概述、第5章 PLC概念（硬件能力）；选型手册v2.8 第16~30页（CPU/扩展模块/信号板规格与接线）

### 第2章 编程基础（98 分钟 · 核心）

**学习目标**：能听懂所有内容；理解相关知识点，为后期编程打好牢固基础；课后题正确率在80%以上

**核心要点**
- 三种编程语言：LAD梯形图（由继电器控制原理图演变、简单易懂、使用最普遍）、FBD功能块图（由数字电路演变）、STL语句表（类似汇编、可实现复杂功能但对基础要求高、使用较少）
- 存储区分类：外部映像区I（数字量输入）、Q（数字量输出）、AI（模拟量输入）、AQ（模拟量输出）；内部存储区V（变量）、M（标志）、T（定时器）、C（计数器）、HC（高速计数器）、AC（累加器）、SM（特殊存储器）、L（局部存储器）、S（顺序控制继电器）
- 数制与表示：二进制2#1100 1000 = 十进制200 = 十六进制16#C8；十六进制字符为0~9和A~F
- 数据类型按长度：BOOL=1个位、BYTE=8位（1字节）、WORD=16位（2字节）、DWORD=32位（4字节）；浮点数只精确到小数点后第6位，输入浮点常数最多指定6位小数，高精度计算结果可能不准确
- 字节/字/双字组合关系：MW0=MB0+MB1、MD0=MB0~MB3；M3.6表示3号字节第6位；16#12345678按DWORD存放时高字16#1234、低字16#5678，MSB在最左
- 三种寻址方式：直接寻址（存储区标识符+数据大小+地址，如I0.0）、符号寻址（用字母数字名访问，如启动按钮）、间接寻址（使用指针访问存储器中的数据）
- 访问格式示例：位V50.5/VB字节VB20/字VW30/双字VD50；直接寻址如3号字节第6位=M3.6、1号字节第3位=V1.3、2和3号字节组合=VW2、1至4号字节组合=VD1
- 符号名命名规则：可包含字母、数字、下划线；第一个字符不能是数字；不能用关键字作符号名；长度不超过23个字符；常量同样可定义符号

**配套练习**：存储区与寻址

**手册出处**：系统手册v2.8 第6章 编程概念（存储器地址语法、数据类型、直接和间接寻址）

### 第3章 学习机（学习箱）介绍（34 分钟 · 拓展）

**学习目标**：记住元件规格和用途；记住元件连接的IO点地址

**核心要点**
- 旋钮：SA1控制学习箱电源（无IO点）、SA2使能直流电机/音乐盒（无IO点）、SA3远程本地切换接I0.5（左旋断开、右旋闭合）
- 按钮：SB1启动（常开，I0.2）、SB2停止（常闭，I0.3，按下断开松开导通）、SB3复位（常开，I0.4）
- 指示灯：黄灯H1=Q0.3、绿灯H2=Q0.5、红灯H3=Q0.6，有输出信号即亮
- 步进电机M1用于运动控制：脉冲信号Q0.0、方向信号Q0.2、使能信号Q0.4；接近开关S1左极限=I1.1、S2原点=I1.2、S3右极限=I1.3（金属接近导通）
- 编码器U1用于高速计数：A相脉冲I0.0、B相脉冲I0.1
- 温度传感器B1接AIW12用于模拟量采集与PID控制；加热棒R1接Q0.7用于PID输出控制
- 直流电机M2接Q0.1学习PWM调速；音乐盒U3共用Q0.1，需发出周期性脉冲才发声（高速脉冲输出练习）
- 温湿度传感器U2不占IO点，采用RS485 MODBUS_RTU通信，用于串口通信练习；数字量输入点表覆盖I0.0~I1.3、输出点表覆盖Q0.0~Q0.7，与电气图纸一一对应

**配套练习**：数字量与模拟量IO、高速计数与脉冲输出、运动控制、PID控制、串口通信

**手册出处**：无直接对应章（以学习箱随箱IO点表与电气图纸为准）；硬件规格可查选型手册v2.8

### 第4章 编程软件介绍（86 分钟 · 核心）

**学习目标**：能听懂所有内容；熟记程序下载步骤；熟记不同系统块参数的区别

**核心要点**
- STEP 7-Micro/WIN SMART安装要求：Windows 7（32/64位）或Windows 10（64位），至少350MB空闲硬盘空间（课程演示为Win10+软件V2.5）
- CPU配置IP规则：勾选IP地址项后CPU的IP只能通过下载程序的方式修改；不勾选则使用CPU原有IP，可任意修改
- 程序下载四步：①网线连接电脑与PLC ②选择网卡驱动 ③搜索PLC的IP地址 ④电脑IP设为与PLC同网段（IP前三位必须相同、最后一位不相同）
- 数字量输入配置：滤波时间指信号在指定时间内保持状态不变才认为有效；需要捕捉很短时间的信号脉冲时须勾选脉冲捕捉
- 数字量输出配置：勾选输出冻结在最后一个状态（指状态变更之前的输出）；勾选指定替代值后CPU处于STOP模式时选定通道输出信号
- 断电保持配置实现CPU断电后数据仍保持断电前数值；CPU安全配置中密码保护知识产权，通信写访问限定通信方式下写CPU数据的范围（PID向导生成的V存储区应在此范围内）
- CPU启动配置：设置重启后CPU工作模式；勾选允许缺少硬件=系统块配置了模块但实际没有时CPU仍运行；勾选允许硬件配置错误=配置与实际模块信息不符时CPU仍运行
- 模拟量输入配置：超出上限（值>32511）、超出下限（值<-32512）；抑制频率越大采样时间越短，滤波周期越多更新时间越长；热电阻配置中电阻类型只采阻值、热敏电阻可直接采温度
- 标准工作流程：系统块配置→程序编写并编译无错误→通信参数配置（选网卡/搜PLC/设IP）→程序下载→程序监控→程序上传

**配套练习**：HMI软件与维护、以太网通信、数字量与模拟量IO

**手册出处**：系统手册v2.8 第7章 PLC设备组态（系统块各选项卡）；第3章 入门指南（程序下载步骤）

### 第5章 入门指令（864 分钟 · 核心）

**学习目标**：能听懂所有内容；熟知每个指令的用法和注意事项、同一类指令之间的区别；要达到给出控制要求立刻就能想到用哪些指令实现；一定要掌握基础指令，程序开发中80%都是基础指令；将所有知识点记录笔记；课后题正确率在80%以上

**核心要点**
- 位逻辑指令：常开触点位值=1导通、常闭触点位值=0导通；输出指令控制变量导通；置位/复位指令带起始地址和数量（最多255个点），触发信号断开后状态保持不变
- 置位复位SR与复位置位RS指令的区别：两输入同时导通时SR置位优先（灯亮）、RS复位优先（灯灭）；取反指令NOT使能流取反
- 定时器TON/TOF/TONR：TON接通延时（IN断开计时清零）、TOF断开延时（IN从1变0开始计时，计到设定值标志位变0）、TONR保持性（IN断开计时保留，需用复位指令R清零）；TON和TOF提供由定时器编号确定的多种分辨率，同一编号不能同时用于TON和TOF（如TON T32与TOF T32）；计时达设定值后继续计时到最大值32767停止
- 计数器CTU/CTD/CTUD：CU增计数、CD减计数、PV预置值、LD装载预置值、R复位；计数范围0~32767，计数器号不能重复使用；计数器有Word（当前值）和Bit（位状态）两种寻址；直接修改当前值不会使标志位变化，必须由输入信号触发
- 存储器范围：I/Q/M均为0.0~31.7；V区VB0~VB8191(ST/SR20)递增到VB20479(ST/SR60)；T0~T255；C0~C255；AIW0~AIW110；AQW0~AQW110；LB0~LB63；SM0.0~SM2047.7
- 传送指令MOV_B/MOV_W/MOV_DW/MOV_R：信号从断开到导通时把数值传送到目标地址（如1234→VW11）；配合上升沿P（断开变导通亮一个周期）/下降沿N（导通变断开亮一个周期）指令实现单周期触发
- 整数运算：ADD_I为16位有符号运算（INT=INT+INT）、ADD_DI为32位（DINT=DINT+DINT）；MUL为16位×16位得32位结果；DIV两个16位整数相除产生32位结果（高16位余数+低16位商）；INC/DEC对IN加/减1送OUT，IN与OUT地址相同时EN必须加上升沿或下降沿；字节递增递减为无符号，字和双字为有符号；超出范围结果不准确
- 浮点运算：ADD_R等为32位运算，浮点数有效位最大7位（如10000000.5+1.0结果不准确）；三角函数输入以弧度为单位，角度转弧度乘0.01745329（π/180）；SQRT求实数平方根（输入IN为REAL）
- 比较指令六种（==、<>、>=、<=、>、<）按数据类型分：字节BYTE（0~255，IB0/QB0/MB0/VB0）、整数INT（-32768~32767，IW0等）、双整数DINT（约±21.47亿，ID0/MD0/VD0）、实数REAL（±3.402823E+38）
- 转换指令：字节转整数无符号扩展；整数转字节仅可转0~255（超范围失败由SM1.1检测）；双整数转整数限-32768~32767；取整ROUND四舍五入（123.789→124）、截取TRUNC舍去小数（123.789→123）；双整数转实数VD0=123→VD10=123.0
- 模拟量工程量换算公式（4~20mA对应5530~27648）：传感器实际值=(传感器最大量程-最小量程)/(转换最大量程-转换最小量程)×(转换实际值-转换最小量程)+传感器最小量程；模拟量输出按同比例反向计算；模拟量输入类型±2.5V/±5V/±10V/0~20mA，抑制频率10/50/60/400Hz，滤波无(1周期)/弱(4)/中(16)/强(32周期)

**配套练习**：基本指令、存储区与寻址、数字量与模拟量IO

**手册出处**：系统手册v2.8 第9章 程序指令（位逻辑、定时器、计数器、传送、数学运算、比较、转换）

### 第6章 编程实践（实战项目）（297 分钟 · 重点）

**学习目标**：学会使用流程图软件梳理自己的逻辑思维；自己可以做出3题以上；熟记编程步骤，建立良好编程习惯；多做案例，达到一看要求就能有编程思路即可

**核心要点**
- 仿真系统使用三前提：CPU的IP地址必须设为192.168.10.123、电脑IP必须设为192.168.10.100、程序必须使用给定的IO点表
- 三级皮带输送机：SB1启动/SB2停止自动运行；启动按时间间隔依次M1→M2→M3，停止依次M3→M2→M1；3号皮带开启打开放料阀M10.3、停止关阀；故障信号M0.1触发时皮带和放料阀立刻全部停止；定时器必须使用100ms分辨率否则仿真系统不准确
- 送料小车自动控制：满料判断决定前进/后退；到卸料位停3s后开卸料阀，卸料完成延时2s关阀；到装料位停3s后装料，装完立刻关阀延时2s再前进；用计数器记录卸料次数VW100，SB2清零；故障时红灯亮并停止自动运行
- 自动门控制：光电开关K1/K2任一触发即电机正转开门，开到位限位停止；开门停留8s后自动关门；关门过程中再检测到人立即停止并重新开门；8s等待期间来人则重新计时8s，保证安全通过；SB2停止后立刻停止所有动作
- 红绿灯控制：南北红灯亮15s同时东西绿灯亮10s，到10s时东西绿灯闪烁3次（亮0.5s灭0.5s）后熄灭，东西黄灯亮2s后切换为东西红灯+南北绿灯（南北绿灯5s+闪烁3次+黄灯2s）；绿色通道使能后对应方向保持常绿
- 供水系统（模拟量项目）：液位传感器4-20mA量程0-100.0mm经AM03模块（装CPU右侧第一个位置）采集，阀门开度4-20mA经AQW16输出；液位<=开泵设定值启泵、>关泵设定值延时3s停泵；1号泵启动后5s内未达标依次启动2号、3号泵；停止顺序1→2→3各延时3s
- 编程方法训练：先用流程图软件梳理控制逻辑，再按仿真IO点表分配M区中间变量（M10.x输出、M20.x反馈），编写后下载仿真验证，建立良好编程步骤与习惯

**配套练习**：基本指令、数字量与模拟量IO

**手册出处**：系统手册v2.8 第9章 程序指令（基础指令综合应用）

### 第7章 程序调试（165 分钟 · 重点）

**学习目标**：能听懂所有内容；亲自尝试所有功能，了解每个功能的用途；将所有知识点记录笔记

**核心要点**
- 固件更新：固件版本低于V2.3的CPU必须使用Micro SD卡更新固件；固件文件包括工作文件S7_JOB.S7S和文件夹FWUPDATE.S7S
- SD存储卡要求：支持商用Micro SD卡；容量支持4G/8G/16G（2G和32G不支持）；必须FAT32文件系统；可用于程序传输、CPU固件更新、恢复CPU出厂设置
- SD卡固件更新/程序传输流程：断电插卡→上电，过程中RUN灯和STOP灯以2Hz频率交替点亮→仅STOP灯闪烁表示操作成功→取下存储卡重新上电查看固件版本
- 强制IO点：RUN模式或STOP模式均可强制（STOP模式下非常危险）；使用强制后STOP指示灯闪烁但程序正常运行；读取强制的IO点需打开状态图表且未开启图表监控
- 交叉引用：使用前须对程序编译且无错误；b=已分配的一个位、B=一个字节、W=一个字（16位）、D=一个双字（32位）、X用于定时器和计数器，用于排查地址重复占用
- 程序比较：软件中的程序与PLC中程序不同时无法在线监控；用于确定程序版本、防止下载失败
- 掉电保持用于保存特殊数据防止断电重启后丢失；数据块用于程序下载时设定变量初始值，书写格式为变量地址+空格+初始值，//后为注释内容
- CPU密码分级（密码区分大小写，可为字母数字符号任意组合）：2级开始限制下载程序；3级开始限制下载和上传程序；4级禁止上传程序，且CPU中已下载过程序块后再次下载时禁止下载系统块
- 忘记密码处理：方法一用Micro SD卡将CPU恢复出厂默认；方法二用软件清除窗口的忘记密码和复位为出厂默认值；单击清除按钮后必须在60秒内以物理方式循环上电，暖启动或其他重启方式无效
- SD卡程序传输细节：CPU做了3级或4级加密时需输入密码才能把程序拷贝到SD卡；用SD卡传输程序相当于先对CPU初始化再下载程序（保留IP地址）；SD卡恢复出厂设置后CPU的IP地址变为出厂IP且CPU名称被删除

**配套练习**：HMI软件与维护

**手册出处**：系统手册v2.8 第12章 调试和故障排除；第7章 PLC设备组态（密码保护、保持范围、存储卡）

## 中级篇

> 中级篇在基础篇之上完成程序结构化（子程序）与进阶指令（时钟/转换/顺控/中断）学习，并掌握PWM、高速计数器、PID、Modbus通信四大典型功能；指令、HSC、PID、Modbus均为初级认证大量出题的核心内容。

### 第8章 编程基础2（子程序）（104 分钟 · 核心）

**学习目标**：1、能听懂所有内容 2、了解子程序的两种用法 3、能够独立编写带参数子程序

**核心要点**
- 子程序用于把整个用户程序按功能结构化组织，利于分步调试、快速定位问题，避免多功能混在一起无法判断问题所在
- 不带参数子程序：调用条件（如M0.0）接通时执行子程序，断开后不再执行，此时子程序内部元件（输出、定时器等）状态可能引发意外，需注意演示中可能出现的问题
- 带参数子程序通过局部存储器L在调用处与子程序之间传递参数，一个子例程最多可传递16个参数
- 局部存储器使用关键原则：先赋值，后使用；参数在局部变量表中定义（符号名最多23个字符）
- 带参数子程序需要多次调用时，子程序内禁止使用上升沿/下降沿指令、计数器、定时器
- 子程序中如果使用置位/复位相关指令，关联的变量类型必须选择IN_OUT（传入/传出），否则多次调用会相互覆盖
- 参数变量类型分IN（传入）、OUT（传出）、IN_OUT（传入传出）、TEMP（临时），TEMP仅在本周期内有效
- 参考资料路径：S7-200 SMART帮助文件/编程基础知识/子程序；技术参考/编程指令/子程序

**配套练习**：基本指令、存储区与寻址

**手册出处**：系统手册v2.8 第9章 程序指令（9.18 子例程）

### 第9章 进阶指令（时钟·转换·顺控·中断）（367 分钟 · 核心）

**学习目标**：1、能听懂所有内容 2、了解指令功能和用法 3、能够看懂并编写出演示程序 4、后期用到能够参考案例和资料编写出来

**核心要点**
- READ_RTC(TODR)从CPU读取当前时间日期装载到T开始的8字节时间缓冲区，SET_RTC(TODW)将8字节缓冲区数据写入CPU；读出的时钟数据为BCD格式，在状态表中用十六进制格式查看
- 时钟使用注意：避免每个扫描周期都读时钟，最小可读时间单位为1秒，可用SM0.5上升沿每秒读取一次；S7-200 SMART时钟精度±120秒/月；CPU长时间断电（无电池信号板最多保持7天）后时钟恢复初始值
- 转换指令：BCD转整数（BCD_I）IN有效范围0~9999的BCD码；整数转BCD（I_BCD）IN有效范围0~9999的整数
- 存储器类型：外部映像区I/Q/AI/AQ，内部存储区V/M/T/C/HC/AC/SM/L/S；顺序控制继电器S的地址范围为S0.0~S31.7
- 顺控（SCR）指令按工艺把复杂步骤分割成若干顺控段顺序执行；当前步未执行时，段内用一般输出(=)指令关联的变量立刻变为0；离开运行步骤后要求输出状态不变时须用置位/复位指令
- 离开顺控运行步骤后，段内延时断开定时器的标志位立刻变为OFF（定时器随之失效），编程时须特别处理
- 中断分3类：通信中断、I/O中断、时基(定时)中断；优先级由高到低为通信＞离散(I/O)＞定时；中断服务程序只能由特定事件触发执行，不能由用户程序调用
- 常用中断事件号：0=I0.0上升沿、1=I0.0下降沿、10=定时中断0(SMB34设定时间间隔)、11=定时中断1(SMB35)、12=HSC0当前值=预设值、21=T32 CT=PT、22=T96 CT=PT、19/20/34=PTO0/1/2脉冲输出完成
- 中断指令：ENI全局启用中断、DISI全局禁止（禁止期间事件仍排队等候）、ATCH将中断事件与中断程序关联并启动、DTCH断开关联并停止、CRETI有条件从中断返回、CLR_EVT清除等候的中断事件；中断相关指令只需触发一次
- 中断例程中不能使用DISI、ENI、HDEF和END指令；采集极短输入信号时可开启脉冲捕捉功能

**配套练习**：基本指令、存储区与寻址

**手册出处**：系统手册v2.8 第9章 程序指令（9.2 时钟 / 9.5 转换 / 9.10 中断 / 9.13 程序控制）

### 第10章 PWM功能（69 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、后期用到能够参考案例和资料编写出来

**核心要点**
- PWM即脉冲宽度调制：周期固定、宽度（占空比）可调的方波输出
- CPU共有3路内置脉冲输出发生器：PWM0对应Q0.0、PWM1对应Q0.1、PWM2对应Q0.3；支持最大脉冲速率100kHz；经济型CPU（CR系列）不支持PWM向导
- 应使用24VDC晶体管输出CPU；继电器型CPU的PWM输出频率不能高于继电器响应频率，且继电器频繁通断会影响CPU使用寿命
- ST20 CPU只有两路高速脉冲输出：向导虽可组态3路PWM，实际只支持2路PWM功能
- PWM配置向导4步：选择脉冲发生器→更改PWM通道名称→组态通道输出时基（毫秒/微秒）→生成项目组件
- PWMx_RUN指令三个关键管脚：EN执行控制、Cycle写入脉冲周期、Pulse写入脉冲宽度
- Cycle取值：时基毫秒时允许2~65535（周期为1时输出只有0/1，失去调节脉宽作用）；时基微秒时允许10~65535（最大频率100kHz，故最小周期10us）
- Pulse脉宽输入为字值，取值范围0~65535个时基单元
- 应用案例电子琴：按音符频率换算周期（低音1=262Hz半周期1908us、中音1=523Hz半周期956us等），动态修改Cycle与Pulse即可发出不同音调

**配套练习**：高速计数与脉冲输出

**手册出处**：系统手册v2.8 第14章 开环运动控制（14.1 使用PWM输出；另见9.7 脉冲输出）

### 第11章 高速计数器（282 分钟 · 核心）

**学习目标**：1、能听懂所有内容 2、了解位置和速度计算实现的原理 3、后期用到能够参考案例和资料编写出来

**核心要点**
- 高速计数器用于计算比普通扫描频率更快的脉冲信号，工作原理与普通计数器类似，但计数通道响应时间更短（1Hz=1个/秒，100Hz=100个/秒）
- 编码器电气选型参数：脉冲数（如600）、供电电压DC24V、输出方式PNP/NPN、信号类型单向/双向/AB相、最大频率100kHz；接线区分PNP型与NPN型输出
- 计数模式4种：单相计数（内部方向控制/外部方向控制）、两相计数（加减双脉冲）、AB相计数、AB相4倍频
- 计数频率：单相HC0/HC1/HC2/HC3=200kHz、HC4/HC5=30kHz（ST30的HC4为200kHz）；两相同单相；AB相HC0/HC2=100kHz、HC4/HC5=20kHz（ST30的HC4为100kHz）
- 向导配置流程：设置输入点滤波时间→选择高速计数器→选择计数模式→初始化配置（预设值/初始值等）→选择需要启用的中断→选择中断步数→中断步骤详细配置→确定生成配置和程序
- HDEF为高速计数器定义指令，选择HSC0~5的工作模式（时钟、方向、复位功能），必须用SM0.1在首次扫描时执行一次；HSC指令根据HSC特殊存储器位组态和控制计数器，参数N指定计数器编号
- SMB36~SMB45是HSC的控制/状态特殊存储器（新当前值、新预设值等控制位只在第一次初始化有效）
- 高速计数器使用步骤：按实际频率在系统块中设置IO点滤波时间→配置计数模式→用向导生成程序→设置参数→启用高速计数器
- 典型应用：位置计算（脉冲数×脉冲当量换算行程）、掉电保持（保持当前计数值）、速度计算（速度=距离/时间，定时读取计数差值）

**配套练习**：高速计数与脉冲输出

**手册出处**：系统手册v2.8 第9章 程序指令（9.6 计数器·9.6.2 高速计数器指令）

### 第12章 PID控制（188 分钟 · 核心）

**学习目标**：1、能听懂所有内容 2、了解PID功能的各种特殊功能 3、后期用到能够参考案例和资料编写出来

**核心要点**
- 开环控制是无反馈控制，闭环控制是有反馈控制；PID三作用：P提高响应速度减少误差但不能消除稳态误差（比例过大使稳定性下降）、I消除稳态误差但使动态响应变慢、D超前调节预测误差趋势、避免严重超调（对噪声有放大作用，滞后对象应加微分）
- 参数整定经验：P由小到大单独调节；调I时先把调好的比例增益降到50%~80%，再由大到小减小积分时间（积分时间越小积分作用越强）
- 比例作用示例：目标液位100cm，加水速度=Kp×(100-实际液位)，Kp=2时液位最终稳定在99.5cm——纯比例控制存在稳态误差
- PID向导流程：定义回路号→为回路组态命名→设定回路参数：增益默认1.00（正/负，正比关系取正、反比关系如抽水泵频率与液位取负）、采样时间默认1.00（必须大于PLC运行周期，向导生成后只能在向导中修改）、积分时间默认10.00（不要积分可设很大如10000）、微分时间默认0.00（不要微分设0）
- 回路过程变量(PV)标定：单极性0~27648（0-10V/0-20mA）、双极性-27648~27648（±10V/±5V）、20%偏移量（4-20mA）为5530~27648且不可改变；设定值SP默认0.0~100.0实数标定，最大可设100000.0
- 回路输出选项：模拟量输出（控制比例阀、变频器等，可再选单极/双极/20%偏移）或数字量输出（输出点按占空比通断，控制固态继电器、加热棒等），选开关量输出时须设定循环周期
- PID指令管脚：EN必须用SM0.0使能且后面不串联任何条件；PV_I填过程反馈的模拟量输入地址；Setpoint填0.0~100.0实数；Auto_M=1为自动；Manual为手动输出0.0~1.0实数（输出百分比）；输出含Output、HightAlarm/LowAlarm高低调报警和Module_Alarm模块错误报警
- PID向导占用定时中断0（SMB34），使用PID向导后其他程序不得再使用该中断、不得向SMB34写入新数值，否则PID停止工作
- 自整定先决条件：回路必须处于自动模式，且整个PID回路工作在相对稳定状态（过程变量接近设定值、输出有规则变化且回路输出值在控制范围中心附近变化）；可通过PID整定控制面板或触摸屏启动自整定
- 手/自动无扰切换：自动→手动时使手动输出值等于当前实际输出；手动→自动时把当前反馈量换算为相应给定值；切换控制程序必须放在PID控制子程序之前

**配套练习**：PID控制

**手册出处**：系统手册v2.8 第13章 PID回路和整定（另见第9章 程序指令 9.9 PID）

### 第13章 Modbus通信（248 分钟 · 核心）

**学习目标**：1、能听懂所有内容 2、了解通信中数据通信机制 3、后期用到能够参考案例和资料编写出来

**核心要点**
- Modbus RTU是单主站主/从通信模式、半双工通信；网络上只能有一个主站且主站在Modbus网络上没有地址；从站地址范围0~247，0为广播地址，实际从站地址1~247（248~255保留，一般用于兼容非标Modbus）
- RS485总线用屏蔽双绞线连接主站与各从站，总线两端需并接终端电阻
- Modbus RTU从站两条指令：MBUS_INIT用于启用/初始化/禁用Modbus通信（用SM0.1调用一次，Mode 1=启动/0=停止）；MBUS_SLAVE用于处理来自主站的请求，必须在每次扫描执行（SM0.0触发），且必须先无错误执行MBUS_INIT；MBUS_SLAVE无输入参数，Done接通时Error有效
- MBUS_INIT关键参数：Addr从站地址1~247、Baud可选1200/2400/4800/9600/19200/38400/57600/115200、Parity 0=无校验/1=奇/2=偶、Port 0=CPU集成RS485/1=信号板RS485或RS232、MAXIQ最大256（0=禁止读写DI/DO）、MAXAI最大56（0=禁止读AI）、MAXHold允许读写的V区字(VW)数量、HoldStart保持寄存器起始地址（&VBx间接寻址）；CPU默认数据位8、停止位1
- Modbus地址与CPU地址映射：00001~09999数字量输出线圈（00001=Q0.0）、10001~19999数字量输入触点（10001=I0.0）、30001~39999输入存储器/模拟量输入（30001=AIW0）、40001~49999保持寄存器（40001=HoldStart指定的VW）
- Modbus RTU主站指令：MBUS_CTRL每扫描周期使能（SM0.0），Mode=1使能Modbus、0恢复PPI协议，Delay为等待从站响应超时（毫秒，典型1000ms，范围1~32767）；执行MBUS_MSG前必须先无错误执行MBUS_CTRL
- MBUS_MSG参数：同一时刻只能有一个读写功能使能，建议用上一条读写指令的Done位激活下一条；First每个新请求必须用脉冲触发；Slave 1~247；RW 0=读/1=写（数字量输出和保持寄存器可读写，数字量输入和模拟量输入只读）；Addr按数据区选择；Count为位或字个数；DataPtr以&VBx间接寻址指定数据区；每条MBUS_MSG最大可读/写120个字
- Modbus TCP通过工业以太网TCP/IP传输，采用客户端-服务器方法，客户端发起连接并请求、服务器响应；S7-200 SMART可作客户端或服务器；客户端指令占用开放式用户通信主动连接资源最多8个，服务器指令占用被动连接资源最多8个
- MBUS_SERVER每次扫描执行；Connect=TRUE允许客户端连接、FALSE断开；IP_Port默认502（范围1~49151，建议2000~5000）；MaxIQ建议256、MaxAI建议56、MaxHold/HoldStart定义可访问的V区；CPU需被多个客户端访问时，要使用多个服务器指令且端口号不同
- MBUS_CLIENT每次扫描都必须调用直到Done为TRUE；Req=TRUE请求通信、Connect=TRUE建立连接/FALSE断开、IPAddr1~4从高到低字节填服务器IP、IP_Port默认502、RW/Addr/Count/DataPtr用法同RTU；实例：温湿度传感器（40001湿度/40002温度，读出数据×0.1为实际值）、V20变频器（40100控制/40101频率设定/40110状态/40111当前频率，从站地址2、4800波特率、无校验）

**配套练习**：串口通信、以太网通信

**手册出处**：系统手册v2.8 第11章 库（11.2 Modbus通信；另见第10章 通信 10.6 RS485）

## 高级篇（第14~22章）

> S7-200 SMART高级篇承接中级篇，聚焦Modbus报文解析、运动控制、数据日志/库、高级指令及S7/PROFINET/自由口/UDP/TCP五大通信专题，是通信与运动控制应用的核心进阶内容；其中通信类章节与运动控制章节均为认证考试出题重点。

### 第14章 Modbus报文（63 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、通过报文查询故障原因

**核心要点**
- 读数据请求帧（功能码03/04）：从站地址(1字节)+功能码(1字节)+起始地址(2字节)+数量(2字节)+CRC校验(2字节)，如 01 03 00 01 00 02 95 CB 表示读1号从站40002~40003；报文中数据格式全部为16进制
- 读数据响应帧（功能码03/04）：从站地址+功能码+字节数量(1字节)+数据内容(N字节)+CRC，如 01 03 04 00 01 00 02 2A 32
- 功能码01/02读数字量状态：01 01 00 00 00 0A BC 0D 读1号从站00001起10个数字量输出，响应 01 01 02 55 03 中 16#55 对应Q0.0~Q0.7、16#03 对应Q1.0~Q1.7 的状态位
- 功能码05写单个线圈：01 05 00 06 FF 00 6C 3B 向1号从站00007地址(对应Q0.6)写1(FF00=ON)；从站返回报文与主站发送的完全相同（回显）
- 功能码06写单个保持寄存器：01 06 00 00 00 7B C9 E9 向40001(对应VW)写入123(16#7B)，从站同样原样回显
- 功能码0F写多个线圈：请求帧多出数量(2字节)+字节数量(1字节)+内容；PLC做从站时起始地址必须位于数字量输出字节界限、数量必须是8的倍数，否则报错
- 功能码10写多个寄存器：01 10 00 00 00 02 04 00 0A 00 0B 92 6A 向40001起写2个16位值(对应VWn和VWn+1)，响应只回起始地址和数量
- 学习目的：通信异常且主从双方都认为自己没问题时，通过分析报文定位故障原因（CRC错误、地址错、功能码不支持等）

**配套练习**：串口通信

**手册出处**：系统手册v2.8 第10章 通信（串口通信-Modbus RTU）

### 第15章 运动控制（355 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、了解每个指令的功能和用法

**核心要点**
- 速度/位置控制原理：每个脉冲使电机转动固定角度，速度由单位时间脉冲数决定；1.8°步进电机转一圈需360/1.8=200脉冲，驱动器32细分后一圈需200×32=6400脉冲
- 方向控制4种方式：单相1输出(P0只接受正向命令)、单相2输出(P0脉冲+P1方向)、双相2输出(P0正向/P1负向)、AB正交2输出；学习箱驱动器为单相2输出（脉冲+方向）
- 轴资源：用本体晶体管输出实现，CPU最多控制3轴(ST20为2轴)，最高100kHz；DIS禁用输出固定为轴0-Q0.4、轴1-Q0.5、轴2-Q0.6，Q0.4无信号=启用控制器（电机锁定），有信号=禁用（电机可自由转动）
- 速度计算：CPU最大脉冲频率100000/s，6400脉冲/圈时电机最大15.625转/s，配导程2mm螺杆最大移动速度31.25mm/s
- 运动向导关键参数：MAX_SPEED范围20Hz~100kHz，SS_SPEED启停速度通常为MAX_SPEED的5%~15%；加速时间满足 Vm=Vs+at；急停补偿即S曲线成型仅适用于一步包络；反冲补偿必须为正值(缺省0)
- 限位/停止输入：响应方式有减速停止和立即停止，触发分电平(Level)和边沿(Edge)；输入滤波时间默认6.4ms，修改滤波时间下载程序后必须给PLC断电重启
- AXISx_CTRL(初始化)：EN用SM0.0常开调用，MOD_EN关闭则电机减速停机；输出C_Pos当前位置、C_Speed当前速度、C_Dir方向(0正向/1反向)、Error错误码
- AXISx_MANUAL(手动)：同一时间仅能启用RUN、JOG_P、JOG_N之一；运行中可修改Speed但不能修改Dir；点动信号持续≥0.5s升到点动速度运行，<0.5s按点动速度移动指定增量
- AXISx_GOTO(位置指令)：Mode 0=绝对位置/1=相对位置/2=单速连续正转/3=单速连续反转；START必须用边沿脉冲触发；Mode=0前必须先用RSEEK或LDPOS建立零位置；运行中修改Pos/Speed/Mode无效
- 参考点：RP_FAST取最大速度的2/3左右、RP_SLOW取启停速度；RP寻找模式有4种（模式3、4需启用零脉冲ZP输入）；RP_OFFSET偏移量必须为正、默认0；RSEEK找参考点、LDOFF改偏移量(须先找到参考点且断电后需重新找)、LDPOS手动创建0点坐标
- 曲线运动：最多32条曲线(编号0~31)，每条曲线最多16步；AXISx_RUN的Profile必须为0~31否则报错；输出C_Profile当前曲线号、C_Step当前步
- 存储与下载：向导分配的V存储区(VB0~VB8191)不能被程序其他部分占用；向导修改了程序块/数据块/系统块，三者必须全部下载到CPU；轴位置可掉电保持，重启后恢复位置并自动创建0点；通过VD7020~VD7088等地址可在线修改组态参数（须掉电保持），修改后需执行读组态块(CFG)指令才生效

**配套练习**：运动控制、高速计数与脉冲输出

**手册出处**：系统手册v2.8 第14章 运动控制

### 第16章 特殊功能（57 分钟 · 拓展）

**学习目标**：1、能听懂所有内容 2、了解数据日志和库的功能和用法

**核心要点**
- 数据日志用于按控制要求把指定数据记录保存到CPU存储器中，便于事后追溯
- 每个数据日志的最大存储容量为2M
- CPU存储器擦除次数有限制，应尽量采用特定信号触发数据记录，避免周期性频繁写入
- 库的导入与删除：通过编程软件的库指令操作将外部库文件倒入项目或从项目中删除
- 创建库_不需分配存储器：如模拟量换算程序制作的库，内部不使用V存储器，调用时无需分配V区
- 创建库_需分配存储器：库程序内部必须用V存储器存数据时，最好采用连续的V地址避免浪费；V存储器必须关联符号，否则会出现错误
- 默认库文件名与库的名称相同；要在生成库的源程序中显示创建的库，必须再新建一个库
- 被加密的库文件如果没有密码，仍可以查看其符号表、变量表和程序注释；将常用重复性程序块制作成库可方便后期直接调用

**配套练习**：HMI软件与维护、存储区与寻址

**手册出处**：系统手册v2.8 第6章 编程概念（数据日志向导）、第11章 库

### 第17章 高级指令（281 分钟 · 拓展）

**学习目标**：1、能听懂所有内容 2、了解指令的功能和用法

**核心要点**
- 通信指令GIP_ADDR/SIP_ADDR：读取/设置CPU的IP地址(ADDR)、子网掩码(MASK)、网关(GATE)；使用SIP_ADDR时系统块以太网端口不能勾选“IP地址数据固定为下面的值”，设置IP建议用沿指令触发
- 逻辑运算指令：取反(对IN求补)、与、或、异或均为按位运算，结果存入OUT
- 块传送BLKMOV：将连续字节/字/双字传送到指定位置，N取值1~255；交换指令SWAP交换字的最高/最低有效字节，常用于通信数据解析，只有一个输入参数须用沿指令触发
- 移位指令SHL/SHR：移位后的空位自动补0；循环移位ROL/ROR：溢出的位自动回补到另一侧；移位完成后最后溢出位为1则SM1.1=1，输出结果为0则SM1.0=1
- 时间捕捉指令（32位、单位ms）：一条捕捉触发那一刻的时间放入OUT，一条计算当前时间与IN时间的差值放入OUT；利用它们可在多次调用的子程序中自制定时器（延时接通、延时断开、保持性延时接通）和替代上升沿/下降沿指令，保持型延时接通复位时需对VD16、VD20清零
- 转换指令：段码SEG按输入值自动转七段显示码(可显示0~F)；解码DECO(IN:BYTE→OUT:WORD)将输出16位中指定位置1其余为0；编码ENCO(IN:WORD→OUT:BYTE)读取最低有效位为1的位号
- 表格指令组：FILL存储器填充（多用于表格初始化清零）、ATT填表、FIFO/LIFO取表、查表；表格第1字VW0=最大数据条目数、VW2=实际条目数，一个表格最多100个数据条目（不含这两个字）；对表格读写操作都要用上升/下降沿触发
- 填表与取表规则：填表依次写入VW4、VW6…并使VW2计数加1；LIFO后进先出取最后数据（原数据仍存在）；FIFO先进先出取VW4且所有数据上移覆盖
- 查表TBL_FIND：TBL连实际条目变量、PIN为查询数据、INDX从指定条目号(从0起)开始并放查询结果（未查到则INDX=实际条目数）；CMD只能填常数：1等于、2不等于、3小于、4大于
- 指针寻址：&VB0代表VB0在内存中的32位地址值，*VD104代表该地址存放的数据值；通过间接寻址可对&VB0+1等地址赋值
- FOR/NEXT循环：INDX当前循环次数、INIT初始值、FINAL最大值，实际循环次数=FINAL-INIT+1；用沿指令触发，可在FOR内部修改FINAL值停止循环；常配合指针寻址求总和/平均值；循环使扫描周期变长，默认最大扫描周期500ms，超时CPU进STOP并提示看门狗超时；END指令可将CPU从RUN切换到STOP模式

**配套练习**：基本指令、存储区与寻址

**手册出处**：系统手册v2.8 第9章 程序指令

### 第18章 S7通信（25 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、了解S7通信的使用场景和用法

**核心要点**
- S7通信常用于PLC之间的数据通信：200SMART可与200、200SMART、300、400、1200、1500等PLC对等交换数据
- 通过S7通信向导添加PUT(写)/GET(读)操作，并为每个操作创建名称和添加注释
- 每个操作需选择类型PUT或GET、设置通信数据长度（最大200字节）、设置远程CPU的IP地址以及本地/远程CPU的通信区域和起始地址
- 程序监控：在状态图表中用二进制格式监控通信状态字（如VB7030、VB7050），其第6位为“通信已激活”标志，正常时0和1不断变换；某一远程伙伴线路断开后该位会一直保持为1
- 多机示例：1号(192.168.10.123)、2号(.124)、3号(.125)三台PLC通过PUT/GET交叉读写VB0~VB9、VB10~VB19、VB50~VB59、VB60~VB69实现读/写数据交换
- 一台CPU可同时做本地和远程：实验中CPU1做本地、CPU2做远程

**配套练习**：以太网通信

**手册出处**：系统手册v2.8 第10章 通信（以太网-S7通信）

### 第19章 PROFINET通信（61 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、了解PROFINET通信的使用场景和用法

**核心要点**
- PROFINET IO基于以太网全双工通信，可实现毫秒级别的发送和接收；系统由PROFINET IO控制器（对远程IO设备最多8个进行数据交互）和PROFINET IO设备（受控制器监控的现场设备，由多个模块/子模块组成）构成
- 200SMART既可做IO设备（智能设备），也可做IO控制器
- 做智能设备：最多添加8个传送器（相当于8个设备），输入类型、输出类型最大长度各128字节；与控制器交换数据存放于IB1152/QB1152开始的128字节（控制器侧为IB128/QB128开始的128字节）
- 智能设备IP地址来源可选：通过软件或指令设定、通过上位控制器设定（断电后IP丢失，重新连上控制器后恢复）、或固定为设定值
- 启动时间行为：启动时间内通信成功则CPU立刻进入运行模式；未成功则时间到后进入运行模式；启动时间设为0则CPU立刻进入运行模式
- 智能设备配置步骤：选择IP地址来源→选择数据传输IO区域→导出GSD文件提供给控制器→点击生成完成配置→程序下载到智能设备CPU
- 做控制器：最多支持8个IO设备，每个IO设备最多8个模块；需先导入IO设备的GSD文件；若同时勾选智能设备说明本CPU上面还有更高级的控制器
- 控制器系统块中IP地址设为固定且不能取消；控制器的站名与智能设备不能相同；最多支持8个智能IO设备
- 更新时间取决于通信数据量大小（一般要大于程序扫描周期），数据量大出现丢包可适当延长；数据保持=通信断开后重新尝试连接的次数；还需组态控制器写入/读取IO设备的数据地址
- 控制器配置步骤：导入GSD→设置控制器IP和站名→添加智能设备→设置智能设备站名和IP→选择数据传输区→生成→下载→搜索智能设备并将其站名改为向导配置中的站名

**配套练习**：以太网通信

**手册出处**：系统手册v2.8 第10章 通信（PROFINET通信）

### 第20章 自由口通信（131 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、了解自由口通信的使用场景和用法（计划表原文误写为PROFINET，按本章内容更正）

**核心要点**
- 自由口用于非工业标准协议的设备（如嵌入式热敏打印机），需自己定义数据格式；电能表等支持Modbus协议的设备则直接用库指令，无需关心报文格式
- 自由口参数：SMB30设置CPU集成RS485口(端口0)，SMB130设置信号板485口(端口1)的波特率/校验等
- 发送指令XMT：TBL指定缓冲区（首字节为发送字符数）；发送完成判断：端口0发送完成中断为事件9、端口1为事件26，或用SM4.5/SM4.6（=1线路空闲，=0正在发送）
- 字符串格式：每个字符占1字节，首字节为字符数，如"ni hao!"6个字符占7个字节；VB1000="当前时间 13:12:34"可直接发送实时时间
- 接收控制字节SMB87(端口0)/SMB187(端口1)：位定义 en、sc、ec、il、c/m、tmr、bk；en=0禁用/en=1启用接收功能
- 起始/结束字符：SMB88/SMB188存放判定消息起始的特定值，SMB89/SMB189存放结束字符特定值；如报文 01 02 03 55 AA BB CC 可用16#55做起始、CC做结束判据
- 空闲线与定时器：SMW90/SMW190为空闲时间设定(ms)，代表接收数据的最小间隔；SMW92/SMW192为字符间/消息间时间(ms)——字符间时间是每收到1字节重新计时、无新字节则停收，消息时间是收到消息后超时停收；tmr=0忽略SMW92/192，tmr=1超时终止接收
- SMB94/SMB194设置允许接收的最大字符数，达到即终止接收；RCV接收指令最多接收255个字符，须用脉冲信号触发
- 禁用/终止接收：SM87.7=0(端口0)或SM187.7=0(端口1)后再调用RCV指令立即终止消息接收；接收完成判断：端口0接收完成中断为事件23、端口1为事件24，或SMB86/SMB186（0=正在接收，非0=终止）
- 接收判别流程：空闲时间→起始字符→字符间超时/结束字符→完成接收；演示案例：收到 FF 01 01 01 EE 校验正确则回发16#FF、错误回发16#00 作为应答

**配套练习**：串口通信

**手册出处**：系统手册v2.8 第10章 通信（串口通信-自由口）

### 第21章 UDP通信（34 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、了解UDP通信的使用场景和用法（计划表原文误写为PROFINET，按本章内容更正）

**核心要点**
- UDP是协议开销最小的简单无连接传输模型，没有握手机制，可靠性仅取决于底层网络，类似“对讲机”通信模式
- 开放式用户通信(OUC)库需要使用50个字节的V存储器；UDP只能做被动连接，因此UDP连接只有8个被动连接
- UDP_CONNECT创建被动连接打开所选本地端口：ConnID范围0~65534（将ID号与本地端口号关联），本地端口建议2000~5000，Req=TRUE启动连接、FALSE显示连接状态
- UDP_SEND发送指令：用沿指令触发，一次发送最多1024字节（DataLen 1~1024）；DataPtr为指向I、Q、M、V存储区的指针（如&VB100）；IPaddr1~4为目的IP的四个八位字节，RemPort远程端口范围1~49151
- UDP_RECV接收指令：一条消息最多接收1024字节；Done=1时Length（实际接收字节数）有效；若消息超出MaxLen缓冲区则被截短并置位Error
- 接收时可从IPaddr1~4和RemPort输出获得发送方的IP地址与端口号
- DISCONNECT断开通道指令：用沿指令触发，按ConnID关闭指定通信通道
- UDP使用步骤：①开启通道（UDP_CONNECT）→②发送或接收信息；可同时与网络调试助手互发数据验证

**配套练习**：以太网通信

**手册出处**：系统手册v2.8 第10章 通信（以太网-UDP）、第11章 库（开放式用户通信）

### 第22章 TCP通信（42 分钟 · 重点）

**学习目标**：1、能听懂所有内容 2、了解TCP通信的使用场景和用法（计划表原文误写为PROFINET，按本章内容更正）

**核心要点**
- TCP在主动设备（发起连接，客户端）和被动设备（接受连接，服务器）之间创建连接，提供可靠、有序并带错误校验的消息发送，类似“电话”通信模式
- 开放式用户通信库需50字节V存储器；开放式用户通信连接资源包括8个主动连接和8个被动连接
- TCP_CONNECT连接指令：Active=TRUE主动连接(客户端)/FALSE被动连接(服务器)；ConnID范围0~65534；本地端口建议2000~5000；被动连接远程端口RemPort设为0；Req触发一次即可
- CPU做服务器注意事项：IP地址设为0则接受任何IP的连接请求，非0则只接受指定IP的请求；可忽略远程端口号（设0）；客户端主动断开后需重新请求连接才能再次连上
- 连接关闭后S7-200 SMART不会自动尝试重连，程序必须再执行一次TCP_CONNECT指令才能重新连接（主动连接和被动连接皆如此）
- TCP_SEND发送指令：用沿指令触发，一次发送最多1024字节（DataLen 1~1024），DataPtr为指向I/Q/M/V区的指针（如&VB100）
- TCP_RECV接收指令：一条消息最多接收1024字节；Done或Error置位时Length（实际接收字节数）才有效，消息超出MaxLen缓冲区被截短并置Error
- DISCONNECT按ConnID关闭指定通信通道（沿触发）；TCP使用步骤：①建立连接→②发送或接收信息；实验中CPU分别作客户端（定时连接服务器直至成功）和服务器（关闭后重新开启允许被连接）与网络调试助手通信

**配套练习**：以太网通信

**手册出处**：系统手册v2.8 第10章 通信（以太网-TCP）、第11章 库（开放式用户通信）
